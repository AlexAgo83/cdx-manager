# cdx-manager

Minimal bootstrap for the `cdx` terminal session manager.

## Commands

- `cdx`
- `cdx add [provider] <name>`
- `cdx rmv <name> --force`
- `cdx <name>`
- `cdx status`
- `cdx status <name>`
- `cdx --help`
- `cdx --version`

## Development

```bash
npm test
```
